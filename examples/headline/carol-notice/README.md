# Carol's notice

`notice.json` is what Carol's client posts to the notice providers for Dana's key after publishing a tag on Dana's print-2 listing. The record under `records/` is the tag as Carol's homeserver serves it. A notice is a pointer: the provider and the recipient both fetch the source and check that it references the target.
