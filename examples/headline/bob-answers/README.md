# Answers from Bob's endpoint

`author-dana.json` answers `author` for Dana's key. `refs-print-2-tags.json` answers `refs` for the print-2 listing, filtered to tags, after seq 5, where the first shared index ended. Both are candidate lists in seq order, with no score or rank.
