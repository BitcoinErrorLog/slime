# Bob's slice of Dana's shop, first snapshot

Bob chose to share Dana's key. This is the published result, a static export signed by Bob's slice key, whose Pubky grant travels in `slice.json`. Every record carries Dana's or the curator's author signature, so a reader checks each copy against its author, not against Bob.
