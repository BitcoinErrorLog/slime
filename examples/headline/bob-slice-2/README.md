# Bob's slice of Dana's shop, second snapshot

The same scope one refresh later. It adds Carol's tag on the print-2 listing, which reached Bob as a notice. set.json names the first snapshot as previous.
