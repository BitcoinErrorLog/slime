# Bob's provider advertisement

Signed by Bob's provider key, the client key (`cnf`) of the Pubky grant in `grant`, which Bob's identity key signed with write on `/pub/slime/`. Published at `pubky://ahj91ipj5suirodjthq7ncnquxdnuemyaicofp7jo1hzam6e5day/pub/slime/providers/pngqa3xbs7baahkgerq1faxhtwrety1z4ezuogtsqw7wk3917rpy.json` through that grant's session. It lists one live endpoint, its roles, the scope Bob chose to share (Dana's key), its latest slice, its notice caps and proof-of-work floor, and one peer. The endpoint uses a reserved example domain.
