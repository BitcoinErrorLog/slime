# Dana's event stream

`events.txt` is Dana's homeserver `/events-stream` output in its SSE format. The listing post is edited to withdrawn at cursor 106 and the shirt post is deleted at cursor 107. `records/` holds the withdrawn version of the listing, and `withdrawn.sig` its author signature, the one version signed after the first.
