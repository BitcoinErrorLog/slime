# Answers from Bob's endpoint

`author-dana-listings.json` answers `author` for Dana's key, filtered to listings. `refs-print-2-tags.json` answers `refs` for the print-2 listing, filtered to tags, after seq 5, which is where the first slice ended. Both are candidate lists in seq order, with no score or rank.
