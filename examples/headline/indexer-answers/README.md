# Signed answers from an indexer

An indexer that Synonym does not run answers two queries: `author` for Dana's key, and `label` for great-print, which finds Carol's tag. Each answer is signed by the indexer's key, whose Pubky grant from the indexer's operator travels in the answer. Each entry carries its record's author signature. `complete` is the indexer's claim.
