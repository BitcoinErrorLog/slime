# Answers from Bob's endpoint

`author-dana.json` answers `author` for Dana's key. `refs-listing-tags.json` answers `refs` for Dana's image post, filtered to tags, after seq 6, where the first slice ended. Both are candidate lists in seq order, with no score or rank.
