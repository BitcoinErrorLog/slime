# Bob's provider advertisement

Signed by Bob's provider key, not by Bob's identity key. It names Bob as operator, one live endpoint, the roles it serves, the scope it retains (Dana's key), its latest slice, and one peer provider a crawler can visit next. The endpoint uses a reserved example domain.
